async function findPrep(){const no=$('svcLookup').value.trim();$('lookupMessage').innerHTML='';if(!no)return alert('Enter the MHelpDesk ticket number first.');await refreshData();const p=state.preps.find(x=>x.ticket_no===no&&x.status==='released');if(!p){const completed=state.preps.find(x=>x.ticket_no===no&&x.status==='closed');$('lookupMessage').innerHTML=completed?'<div class="warn"><b>Equipment verification for MHelpDesk Ticket #'+esc(no)+' is already complete.</b></div>':'<div class="bad"><b>No released IT equipment prep matches MHelpDesk Ticket #'+esc(no)+'.</b><div class="small">Confirm the ticket number or contact IT.</div></div>';return}if(!state.matched.includes(p.id))state.matched.push(p.id);$('svcLookup').value='';$('lookupMessage').innerHTML='<div class="ok"><b>Matched MHelpDesk Ticket #'+esc(p.ticket_no)+'</b> to IT-prepared equipment.</div>';renderMatched();updateMorningStatus()}
function exactId(item){return 'exact_'+item.id}function battInputId(item){return 'sbatt_'+item.id}function battCheckId(item){return 'sbattok_'+item.id}
function serviceItem(item){const req=requiredBattery(item),b=BATTERY[item.equipment_type];return '<div class="unitConfirm"><div class="row">'+badge(item.purpose)+' <b>'+esc(eqLabel(item.equipment_type))+' — exact unit '+esc(item.unit_tag)+'</b></div>'+(item.purpose==='DELIVERY'?'<div class="small">IT completed the DELIVERY readiness checks before release.</div>':'')+'<div class="check"><input id="'+exactId(item)+'" type="checkbox"><div><b>I physically have this exact unit tag.</b></div></div>'+(req>0?'<div class="batteryVerify"><b>Battery Checkout Verification</b><div class="small">Required: <b>'+req+'</b> × '+esc(b.label)+' · IT prepared: <b>'+item.battery_count+'</b></div><label style="margin-top:9px">Battery / battery-box count physically in hand</label><input id="'+battInputId(item)+'" type="number" min="'+req+'" inputmode="numeric" placeholder="Enter count"><div class="check"><input id="'+battCheckId(item)+'" type="checkbox"><div><b>I physically counted and verified the required batteries / battery box.</b></div></div></div>':'')+'</div>'}
function renderMatched(){if(!['service','owner'].includes(state.profile?.role||''))return;const preps=state.matched.map(id=>state.preps.find(p=>p.id===id)).filter(p=>p&&p.status==='released');$('matchedPreps').innerHTML=preps.length?preps.map(p=>'<div class="item prepared"><div class="ticketHead"><div><b>MHelpDesk Ticket #'+esc(p.ticket_no)+'</b><div class="small">'+esc(p.site||'No site')+'</div></div><span class="pill green">MATCHED TO IT EQUIPMENT PREP</span></div>'+[...(p.prep_items||[])].sort((a,b)=>a.item_order-b.item_order).map(serviceItem).join('')+'<div class="row" style="margin-top:10px"><button class="mini danger" onclick="unmatchTicket(\''+p.id+'\')">Remove From Morning View</button></div><button class="btn" onclick="closePreparedTicket(\''+p.id+'\')">Complete Equipment Checkout Verification</button></div>').join(''):'<div class="warn">No equipment prep matched yet. Enter an MHelpDesk ticket number above if the job needs BACKUP, SWAP, or DELIVERY equipment.</div>';renderSessionClosed()}
function unmatchTicket(id){state.matched=state.matched.filter(x=>x!==id);renderMatched();updateMorningStatus()}
async function closePreparedTicket(id){const p=state.preps.find(x=>x.id===id);if(!p)return;const verifications=[];for(const item of [...(p.prep_items||[])].sort((a,b)=>a.item_order-b.item_order)){const exact=$('exact_'+item.id);if(!exact?.checked)return alert('Confirm the exact unit tag '+item.unit_tag+'.');let battery_count=0,battery_verified=false;const req=requiredBattery(item);if(req>0){const inp=$('sbatt_'+item.id),chk=$('sbattok_'+item.id);if(!inp||inp.value==='')return alert('Enter the battery count for '+eqLabel(item.equipment_type)+' '+item.unit_tag+'.');battery_count=Number(inp.value);battery_verified=!!chk?.checked;if(battery_count!==Number(item.battery_count))return alert('Battery count must match IT prepared count: '+item.battery_count+'.');if(battery_count<req)return alert(eqLabel(item.equipment_type)+' requires at least '+req+' '+BATTERY[item.equipment_type].label+'.');if(!battery_verified)return alert('Check the battery verification box for '+item.unit_tag+'.')}verifications.push({item_id:item.id,unit_tag:item.unit_tag,unit_confirmed:true,battery_count,battery_verified})}setBusy(true);const {error}=await db.rpc('close_prep_ticket',{p_prep_id:id,p_verifications:verifications});setBusy(false);if(error)return alert(error.message);state.sessionClosed.push(p.ticket_no);state.matched=state.matched.filter(x=>x!==id);await refreshData();renderSessionClosed();alert('Equipment checkout verification for MHelpDesk Ticket #'+p.ticket_no+' is complete and recorded.')}
function renderSessionClosed(){$('sessionClosed').innerHTML=state.sessionClosed.length?'<div class="ok"><b>Equipment verified this morning</b><div class="small">'+state.sessionClosed.map(n=>'MHelpDesk Ticket #'+esc(n)).join(' · ')+'</div></div>':''}
function truckComplete(){const boxes=[...document.querySelectorAll('.truckBox')];return boxes.length===8&&boxes.every(x=>x.checked)}function trailerComplete(){if(!$('takingTrailer').checked)return true;const boxes=[...document.querySelectorAll('.trailerBox')];return boxes.length===7&&boxes.every(x=>x.checked)}
function toggleTrailer(){$('trailerArea').classList.toggle('hidden',!$('takingTrailer').checked);updateMorningStatus()}
function updateMorningStatus(){if(!['service','owner'].includes(state.profile?.role||''))return;const issues=[];if(!$('mhelpReviewed').checked)issues.push('MHelpDesk jobs not reviewed');if(state.matched.length)issues.push(state.matched.length+' matched equipment verification'+(state.matched.length>1?'s':'')+' still incomplete');if(!truckComplete())issues.push('Truck inspection incomplete');if(!trailerComplete())issues.push('Trailer inspection incomplete');$('truckStatus').innerHTML=issues.length?'<div class="warn"><b>Morning check not complete</b><div class="small">'+issues.map(esc).join('<br>')+'</div></div>':'<div class="ok"><b>READY TO SUBMIT MORNING CHECK</b></div>'}
async function submitMorning(){msg('morningMessage','');if(!$('mhelpReviewed').checked)return msg('morningMessage','Confirm that you reviewed today’s MHelpDesk jobs.','bad');if(state.matched.length)return msg('morningMessage','Complete or remove every matched equipment verification first.','bad');if(!truckComplete())return msg('morningMessage','Complete all 8 truck checks.','bad');if(!trailerComplete())return msg('morningMessage','Complete all 7 trailer checks.','bad');const truck={};document.querySelectorAll('.truckBox').forEach((x,i)=>truck['truck_'+(i+1)]=x.checked);const trailer={};document.querySelectorAll('.trailerBox').forEach((x,i)=>trailer['trailer_'+(i+1)]=x.checked);setBusy(true);const {error}=await db.rpc('submit_morning_check',{p_mhelp_reviewed:true,p_truck_checks:truck,p_taking_trailer:$('takingTrailer').checked,p_trailer_checks:trailer,p_closed_ticket_nos:state.sessionClosed});setBusy(false);if(error)return msg('morningMessage',error.message,'bad');msg('morningMessage','Morning readiness check submitted to the Owner.','ok');state.sessionClosed=[];resetMorningInputs();await refreshData()}
function resetMorningInputs(){$('mhelpReviewed').checked=false;$('takingTrailer').checked=false;$('trailerArea').classList.add('hidden');document.querySelectorAll('.truckBox,.trailerBox').forEach(x=>x.checked=false);$('lookupMessage').innerHTML='';renderSessionClosed();updateMorningStatus()}
