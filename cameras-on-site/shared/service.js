async function findPrep(){const no=$('svcLookup').value.trim();$('lookupMessage').innerHTML='';if(!no)return alert('Enter the MHelpDesk ticket number first.');await refreshData();const p=state.preps.find(x=>x.ticket_no===no&&x.status==='released');if(!p){const closed=state.preps.find(x=>x.ticket_no===no&&x.status==='closed');$('lookupMessage').innerHTML=closed?'<div class="warn"><b>Ticket #'+esc(no)+' is already closed.</b></div>':'<div class="bad"><b>No released IT prep matches Ticket #'+esc(no)+'.</b><div class="small">Confirm the ticket number or contact IT.</div></div>';return}if(!state.matched.includes(p.id))state.matched.push(p.id);$('svcLookup').value='';$('lookupMessage').innerHTML='<div class="ok"><b>Matched Ticket #'+esc(p.ticket_no)+'</b> to IT preparation.</div>';renderMatched();updateMorningStatus()}
function exactId(item){return 'exact_'+item.id}function battInputId(item){return 'sbatt_'+item.id}function battCheckId(item){return 'sbattok_'+item.id}
function serviceItem(item){const b=BATTERY[item.equipment_type];return '<div class="unitConfirm"><div class="row">'+badge(item.purpose)+' <b>'+esc(item.equipment_type)+' — exact unit '+esc(item.unit_tag)+'</b></div><div class="check"><input id="'+exactId(item)+'" type="checkbox"><div><b>I physically have this exact unit tag.</b></div></div>'+(b?'<div class="batteryVerify"><b>Battery Checkout Verification</b><div class="small">IT prepared: <b>'+item.battery_count+'</b> × '+esc(b.label)+'</div><label style="margin-top:9px">Battery count physically in hand</label><input id="'+battInputId(item)+'" type="number" min="0" inputmode="numeric" placeholder="Enter count"><div class="check"><input id="'+battCheckId(item)+'" type="checkbox"><div><b>I physically counted and verified these batteries.</b></div></div></div>':'')+'</div>'}
function renderMatched(){if(!['service','owner'].includes(state.profile?.role||''))return;const preps=state.matched.map(id=>state.preps.find(p=>p.id===id)).filter(p=>p&&p.status==='released');$('matchedPreps').innerHTML=preps.length?preps.map(p=>'<div class="item prepared"><div class="ticketHead"><div><b>Ticket #'+esc(p.ticket_no)+'</b><div class="small">'+esc(p.site||'No site')+'</div></div><span class="pill green">MATCHED TO IT</span></div>'+[...(p.prep_items||[])].sort((a,b)=>a.item_order-b.item_order).map(serviceItem).join('')+'<div class="row" style="margin-top:10px"><button class="mini danger" onclick="unmatchTicket(\''+p.id+'\')">Remove From Morning View</button></div><button class="btn" onclick="closePreparedTicket(\''+p.id+'\')">Verify Checkout & Close Prepared Ticket</button></div>').join(''):'<div class="warn">No ticket matched yet. Enter an MHelpDesk ticket number above if the job needs a BACKUP or SWAP.</div>';renderSessionClosed()}
function unmatchTicket(id){state.matched=state.matched.filter(x=>x!==id);renderMatched();updateMorningStatus()}
async function closePreparedTicket(id){const p=state.preps.find(x=>x.id===id);if(!p)return;const verifications=[];for(const item of [...(p.prep_items||[])].sort((a,b)=>a.item_order-b.item_order)){const exact=$('exact_'+item.id);if(!exact?.checked)return alert('Confirm the exact unit tag '+item.unit_tag+'.');let battery_count=0,battery_verified=false;if(BATTERY[item.equipment_type]){const inp=$('sbatt_'+item.id),chk=$('sbattok_'+item.id);if(!inp||inp.value==='')return alert('Enter the battery count for '+item.equipment_type+' '+item.unit_tag+'.');battery_count=Number(inp.value);battery_verified=!!chk?.checked;if(battery_count!==Number(item.battery_count))return alert('Battery count must match IT prepared count: '+item.battery_count+'.');if(!battery_verified)return alert('Check the battery verification box for '+item.unit_tag+'.')}verifications.push({item_id:item.id,unit_tag:item.unit_tag,unit_confirmed:true,battery_count,battery_verified})}setBusy(true);const {error}=await db.rpc('close_prep_ticket',{p_prep_id:id,p_verifications:verifications});setBusy(false);if(error)return alert(error.message);state.sessionClosed.push(p.ticket_no);state.matched=state.matched.filter(x=>x!==id);await refreshData();renderSessionClosed();alert('Ticket #'+p.ticket_no+' checkout is closed and recorded.')}
function renderSessionClosed(){$('sessionClosed').innerHTML=state.sessionClosed.length?'<div class="ok"><b>Closed this morning</b><div class="small">'+state.sessionClosed.map(n=>'Ticket #'+esc(n)).join(' · ')+'</div></div>':''}
function truckComplete(){const boxes=[...document.querySelectorAll('.truckBox')];return boxes.length===8&&boxes.every(x=>x.checked)}function trailerComplete(){if(!$('takingTrailer').checked)return true;const boxes=[...document.querySelectorAll('.trailerBox')];return boxes.length===7&&boxes.every(x=>x.checked)}
function toggleTrailer(){$('trailerArea').classList.toggle('hidden',!$('takingTrailer').checked);updateMorningStatus()}
function updateMorningStatus(){if(!['service','owner'].includes(state.profile?.role||''))return;const issues=[];if(!$('mhelpReviewed').checked)issues.push('MHelpDesk jobs not reviewed');if(state.matched.length)issues.push(state.matched.length+' matched ticket'+(state.matched.length>1?'s':'')+' still open');if(!truckComplete())issues.push('Truck inspection incomplete');if(!trailerComplete())issues.push('Trailer inspection incomplete');$('truckStatus').innerHTML=issues.length?'<div class="warn"><b>Morning check not complete</b><div class="small">'+issues.map(esc).join('<br>')+'</div></div>':'<div class="ok"><b>READY TO SUBMIT MORNING CHECK</b></div>'}
async function submitMorning(){msg('morningMessage','');if(!$('mhelpReviewed').checked)return msg('morningMessage','Confirm that you reviewed today’s MHelpDesk jobs.','bad');if(state.matched.length)return msg('morningMessage','Close or remove every matched prepared ticket first.','bad');if(!truckComplete())return msg('morningMessage','Complete all 8 truck checks.','bad');if(!trailerComplete())return msg('morningMessage','Complete all 7 trailer checks.','bad');const truck={};document.querySelectorAll('.truckBox').forEach((x,i)=>truck['truck_'+(i+1)]=x.checked);const trailer={};document.querySelectorAll('.trailerBox').forEach((x,i)=>trailer['trailer_'+(i+1)]=x.checked);setBusy(true);const {error}=await db.rpc('submit_morning_check',{p_mhelp_reviewed:true,p_truck_checks:truck,p_taking_trailer:$('takingTrailer').checked,p_trailer_checks:trailer,p_closed_ticket_nos:state.sessionClosed});setBusy(false);if(error)return msg('morningMessage',error.message,'bad');msg('morningMessage','Morning check submitted to the Owner.','ok');state.sessionClosed=[];resetMorningInputs();await refreshData()}
function resetMorningInputs(){$('mhelpReviewed').checked=false;$('takingTrailer').checked=false;$('trailerArea').classList.add('hidden');document.querySelectorAll('.truckBox,.trailerBox').forEach(x=>x.checked=false);$('lookupMessage').innerHTML='';renderSessionClosed();updateMorningStatus()}
