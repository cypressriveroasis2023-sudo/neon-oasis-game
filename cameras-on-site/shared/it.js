function toggleReconBatteryInput(){const show=$('needType')?.value==='Recon 2';$('reconBatteryWrap')?.classList.toggle('hidden',!show)}
function addNeed(){const type=$('needType').value,purpose=$('needPurpose').value,qty=Math.max(1,Number($('needQty').value||1));const battery_qty=type==='Recon 2'?Math.max(1,Number($('reconBatteryQty').value||1)):null;const old=draftNeeds.find(x=>x.equipment_type===type&&x.purpose===purpose&&Number(x.battery_qty||0)===Number(battery_qty||0));if(old)old.qty+=qty;else draftNeeds.push({equipment_type:type,purpose,qty,battery_qty});renderDraftNeeds()}
function removeNeed(i){draftNeeds.splice(i,1);renderDraftNeeds()}
function needBatteryText(n){const b=BATTERY[n.equipment_type];if(!b)return '';const count=n.equipment_type==='Recon 2'?Number(n.battery_qty||1):Number(b.per||0);return ' · '+count+' × '+esc(b.label)+' per unit'}
function renderDraftNeeds(){$('needDraft').innerHTML=draftNeeds.map((n,i)=>'<div class="item"><div class="row">'+badge(n.purpose)+' <b>'+n.qty+' × '+esc(eqLabel(n.equipment_type))+'</b><button class="mini danger" onclick="removeNeed('+i+')">Remove</button></div><div class="small">MHelpDesk quantity: '+n.qty+needBatteryText(n)+'</div></div>').join('')}
async function createPrep(){msg('itCreateMessage','');const ticket=$('itTicket').value.trim(),site=$('itSite').value.trim();if(!ticket)return msg('itCreateMessage','Enter the MHelpDesk ticket number.','bad');if(!draftNeeds.length)return msg('itCreateMessage','Add at least one BACKUP, SWAP, or DELIVERY requirement.','bad');setBusy(true);const {error}=await db.rpc('create_it_prep',{p_ticket_no:ticket,p_site:site,p_requirements:draftNeeds});setBusy(false);if(error)return msg('itCreateMessage',error.message,'bad');$('itTicket').value='';$('itSite').value='';draftNeeds=[];renderDraftNeeds();msg('itCreateMessage','IT equipment prep created. Verify the exact units below.','ok');await refreshData()}
function deliveryReady(item){return item.purpose!=='DELIVERY'||(item.delivery_sim_ok&&item.delivery_camera_app_ok&&item.delivery_batteries_charged_ok&&item.delivery_monitoring_ok&&item.delivery_ticket_count_ok)}
function itemReady(item){return !!item.unit_tag&&!!item.verified_at&&deliveryReady(item)}
function checked(v){return v?' checked':''}
function deliveryChecklist(item){if(item.purpose!=='DELIVERY')return '';return '<div class="deliveryChecks"><div class="subhead">DELIVERY Readiness</div><div class="small">All five checks are required before this delivery equipment can be released.</div><div class="check"><input id="sim_'+item.id+'" type="checkbox"'+checked(item.delivery_sim_ok)+'><div><b>SIM card active and installed in router</b></div></div><div class="check"><input id="cam_'+item.id+'" type="checkbox"'+checked(item.delivery_camera_app_ok)+'><div><b>Camera is visible in the camera app</b></div></div><div class="check"><input id="charged_'+item.id+'" type="checkbox"'+checked(item.delivery_batteries_charged_ok)+'><div><b>Batteries / battery box charged and ready</b></div></div><div class="check"><input id="monitor_'+item.id+'" type="checkbox"'+checked(item.delivery_monitoring_ok)+'><div><b>Central Station monitoring created and sent in</b></div></div><div class="check"><input id="count_'+item.id+'" type="checkbox"'+checked(item.delivery_ticket_count_ok)+'><div><b>This unit is included in the equipment type and quantity listed on the MHelpDesk ticket</b></div></div></div>'}
function itemForm(item){const req=requiredBattery(item),b=BATTERY[item.equipment_type];if(itemReady(item))return '<div class="ok"><div class="row">'+badge(item.purpose)+' <b>'+esc(eqLabel(item.equipment_type))+' — '+esc(item.unit_tag)+'</b></div><div class="small">Verified by IT'+(req>0?' · Prepared: '+item.battery_count+' × '+esc(b.label):'')+(item.purpose==='DELIVERY'?' · Delivery readiness complete':'')+'</div></div>';return '<div class="unitForm"><div class="row">'+badge(item.purpose)+' <b>'+esc(eqLabel(item.equipment_type))+'</b></div><label style="margin-top:8px">Exact Unit Tag</label><input id="tag_'+item.id+'" value="'+esc(item.unit_tag||'')+'" placeholder="Unit tag">'+(req>0?'<label style="margin-top:8px">Batteries / Battery Box Prepared</label><input id="batt_'+item.id+'" type="number" min="'+req+'" value="'+(item.battery_count||req)+'"><div class="small">Required by this MHelpDesk prep: '+req+' × '+esc(b.label)+'</div>':'')+'<div class="check"><input id="power_'+item.id+'" type="checkbox"'+checked(item.power_ok)+'><div><b>Powers on correctly</b></div></div><div class="check"><input id="func_'+item.id+'" type="checkbox"'+checked(item.functions_ok)+'><div><b>Functions tested and working</b></div></div><div class="check"><input id="safe_'+item.id+'" type="checkbox"'+checked(item.safe_ok)+'><div><b>Safe for field use</b></div></div>'+deliveryChecklist(item)+'</div>'}
function renderIT(){if(!['it','owner'].includes(state.profile?.role||''))return;const open=state.preps.filter(p=>p.status!=='closed').length;$('openPrepCount').textContent=open+' open';$('itPreps').innerHTML=state.preps.length?state.preps.map(p=>{const st=p.status==='draft'?'<span class="pill amber">IT EQUIPMENT PREP</span>':p.status==='released'?'<span class="pill green">READY FOR SERVICE CHECKOUT</span>':'<span class="pill">EQUIPMENT VERIFIED</span>';const sorted=[...(p.prep_items||[])].sort((a,b)=>a.item_order-b.item_order);const items=sorted.map(itemForm).join('');let action='';if(p.status==='draft'){const pending=sorted.filter(i=>!itemReady(i)).length;action='<button class="btn" onclick="saveAndRelease(\''+p.id+'\')">'+(pending?'Verify '+pending+' Item'+(pending>1?'s':'')+' & Release Equipment to Service Tech':'Release Equipment to Service Tech')+'</button>'}return '<div class="item prepared '+(p.status==='closed'?'closed':'')+'"><div class="ticketHead"><div><b>MHelpDesk Ticket #'+esc(p.ticket_no)+'</b><div class="small">'+esc(p.site||'No site')+'</div></div>'+st+'</div>'+items+action+'</div>'}).join(''):'<div class="warn">No IT equipment prep records yet.</div>'}
async function saveAndRelease(prepId){const p=state.preps.find(x=>x.id===prepId);if(!p||p.status!=='draft')return;const pending=[...(p.prep_items||[])].filter(i=>!itemReady(i)).sort((a,b)=>a.item_order-b.item_order);setBusy(true);for(const item of pending){const tag=$('tag_'+item.id)?.value.trim()||'';const power=$('power_'+item.id)?.checked||false,func=$('func_'+item.id)?.checked||false,safe=$('safe_'+item.id)?.checked||false;const req=requiredBattery(item);const batt=req>0?Number($('batt_'+item.id)?.value||0):0;if(!tag||!power||!func||!safe){setBusy(false);return alert('Complete the unit tag and all three IT equipment checks for '+eqLabel(item.equipment_type)+' before release.')}if(req>0&&batt<req){setBusy(false);return alert(eqLabel(item.equipment_type)+' requires '+req+' '+BATTERY[item.equipment_type].label+'.')}const {error}=await db.rpc('verify_prep_item',{p_item_id:item.id,p_unit_tag:tag,p_battery_count:batt,p_power_ok:power,p_functions_ok:func,p_safe_ok:safe});if(error){setBusy(false);await refreshData();return alert(error.message)}if(item.purpose==='DELIVERY'){const sim=$('sim_'+item.id)?.checked||false,cam=$('cam_'+item.id)?.checked||false,charged=$('charged_'+item.id)?.checked||false,monitor=$('monitor_'+item.id)?.checked||false,count=$('count_'+item.id)?.checked||false;if(!sim||!cam||!charged||!monitor||!count){setBusy(false);await refreshData();return alert('Complete all five DELIVERY readiness checks for '+eqLabel(item.equipment_type)+' '+tag+'.')}const {error:deliveryError}=await db.rpc('verify_delivery_item_checks',{p_item_id:item.id,p_sim_ok:sim,p_camera_app_ok:cam,p_batteries_charged_ok:charged,p_monitoring_ok:monitor,p_ticket_count_ok:count});if(deliveryError){setBusy(false);await refreshData();return alert(deliveryError.message)}}}const {error:releaseError}=await db.rpc('release_prep',{p_prep_id:prepId});setBusy(false);if(releaseError){await refreshData();return alert(releaseError.message)}await refreshData();alert('Equipment for MHelpDesk Ticket #'+p.ticket_no+' is released to Service Tech checkout.')}
